@{
    Layout = "~/Views/Shared/BootStrap/_LayoutBS.cshtml";
}



<p><code>&lt;header&gt;</code> 作为内联元素被包围。</p>
<p>如果需要把代码显示为一个独立的块元素，请使用 &lt;pre&gt; 标签：</p>
<pre>
    &lt;article&gt;
        &lt;h1&gt;Article Heading&lt;/h1&gt;
    &lt;/article&gt;
</pre>

<h2>代码</h2>
<p>使用 var 元素表示变量：</p>
<p><var>x</var> = <var>a</var><var>b</var> + <var>y</var>
</p>
这个不知道为什么我感觉有点没什么用处。 var
<p>
   <em>x= ab + y</em> 
</p>
